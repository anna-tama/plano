rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /registros/{document=**} {
      allow read: if true;     // Todos pueden leer
      allow write: if true;    // Temporalmente permitido (luego cambia esto)
    }
  }
}